{
  "_comment": "PER-BOT config — install at ~/.codex-channel-octo/<id>/config.json (the <id> must match an entry in the global config's `bots` list; for a single bot use id 'default'). This file is the highest-priority layer: it overrides both the inline bots[] entry and the global shared fields. It holds the bot's REQUIRED botToken. The bot's directories are derived automatically as siblings of this file: ./data, ./workspace, ./codex-home. Drop a ./SOUL.md next to this file to give the bot a personality (it overrides sdk.systemPrompt).",
  "botToken": "bf_YOUR_BOT_TOKEN",

  "_comment_overrides": "Any of these optional fields override the global config for THIS bot only:",
  "_comment_apiUrl": "apiUrl — override the Octo API base for this bot.",

  "sdk": {
    "_comment_model": "Override the model for this bot, e.g. 'gpt-5.5'. Omit to use the codex provider default.",
    "_comment_systemPrompt": "Inline personality string. A ./SOUL.md file next to this config overrides it. Written to the session AGENTS.md together with the security prefix.",
    "_comment_codex_home": "Per-bot CODEX_HOME (codex auth/config/sessions root). Defaults to ./codex-home (isolated — IM content stays out of your personal ~/.codex; authenticate it via `codex login` with CODEX_HOME set, or set sdk.codexApiKey). OPT-IN SHARING: set this to your personal ~/.codex to let IM threads be resumed from your own terminal — note IM content then lands in your personal codex sessions.",
    "_comment_env": "Extra env injected into the codex subprocess (on top of CODEX_HOME), e.g. provider routing vars."
  }
}
